import { ComponentSingleStyleConfig, extendTheme } from '@chakra-ui/react'

export const Switch: ComponentSingleStyleConfig = {
  baseStyle: {
    track: {
      _checked: {
        bg: 'brand.crabNebulaLight',
      },
    },
  },
}
